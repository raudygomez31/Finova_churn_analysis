SELECT 
    Geography AS Pais,
    COUNT(CustomerId) AS Total_Clientes,
    SUM(Exited) AS Clientes_Perdidos,
    ROUND((SUM(Exited) * 100.0) / COUNT(CustomerId), 2) AS Porcentaje_Churn
FROM ClientesFinova
GROUP BY Geography
ORDER BY Porcentaje_Churn DESC;
